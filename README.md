# Universal-Table-Extractor
A fast, universal table extraction tool for PDFs, scanned documents, images, and PPTX files. Uses a smart pipeline: pdfplumber/python-pptx first, then Azure OCR fallback. Handles complex multi-header tables and outputs clean, structured JSON/CSV for analytics and automation.
